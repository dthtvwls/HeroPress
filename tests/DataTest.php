<?php
namespace HeroPress;

class DataTest extends \PHPUnit_Framework_TestCase {
  static function setUpBeforeClass() {
    //(new Data('sqlite::memory:'))->loadSchema();
  }

  function testUpsertSelect() {
    /*$this->assertEquals(Data::upsert('foo',  'bar'), 201);
    $this->assertEquals(Data::select('foo'), 'bar');
    $this->assertEquals(Data::upsert('foo',  'baz'), 200);
    $this->assertEquals(Data::select('foo'), 'baz');*/
  }
}
